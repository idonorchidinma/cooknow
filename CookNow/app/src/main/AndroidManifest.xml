<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tony.cooknow">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".InventoryActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DairyActivity" />
        <activity android:name=".MeatActivity" />
        <activity android:name=".VegetablesActivity" />
        <activity android:name=".FruitActivity" />
        <activity android:name=".RecipesActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".FavoritesActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".OtherActivity"></activity>
        <activity android:name=".AddIngredientActivity"></activity>
    </application>

</manifest>
